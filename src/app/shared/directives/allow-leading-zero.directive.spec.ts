import { AllowLeadingZeroDirective } from './allow-leading-zero.directive';

describe('AllowLeadingZeroDirective', () => {
  it('should create an instance', () => {
    // const directive = new AllowLeadingZeroDirective();
    // expect(directive).toBeTruthy();
  });
});
